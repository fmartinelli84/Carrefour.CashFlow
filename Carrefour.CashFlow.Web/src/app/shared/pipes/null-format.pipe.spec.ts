import { NullFormatPipe } from './null-format.pipe';

describe('NullFormatPipe', () => {
  it('create an instance', () => {
    const pipe = new NullFormatPipe();
    expect(pipe).toBeTruthy();
  });
});
