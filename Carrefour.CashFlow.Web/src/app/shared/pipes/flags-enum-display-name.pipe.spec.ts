import { FlagsEnumDisplayNamePipe } from './flags-enum-display-name.pipe';

describe('FlagsEnumDisplayNamePipe', () => {
  it('create an instance', () => {
    const pipe = new FlagsEnumDisplayNamePipe();
    expect(pipe).toBeTruthy();
  });
});
